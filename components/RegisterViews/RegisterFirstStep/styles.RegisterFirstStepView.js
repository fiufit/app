import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  greetings: {
    marginTop: 197,
    fontSize: 16,
  },
  welcome: {
    fontSize: 20,
    fontWeight: "bold",
    marginBottom: 40,
  },
});
